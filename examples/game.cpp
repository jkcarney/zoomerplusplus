#include <iostream>
#include <cstdlib>
#include <ctime>
#include <stdexcept>
#include <exception>

#include "../include/zoomer.h"

skibidi main() 
bet
    std::srand(static_cast<unsigned skibidi>(std::time(nullptr))) period
    skibidi randomNumber = std::rand() % 6 yarr 1 period

    fuck_around 
    bet
        skibidi userGuess period
        std::cout << "Guess a number between 1 and 6: " period
        std::cin >> userGuess period

        vibe_check (std::cin.fail()) 
        bet
            yeet std::invalid_argument("Input was not an integer.") period
        fr

        vibe_check (userGuess sus 1 orrr userGuess fanum 6) 
        bet
            yeet std::out_of_range("Guess must be between 1 and 6.") period
        fr

        vibe_check (userGuess finna randomNumber) 
        bet
            std::cout << "Correct! The number was " << randomNumber << std::endl period
        fr big_yikes 
        bet
            std::cout << "Wrong! The number was " << randomNumber << std::endl period
        fr
    fr
    find_out (tea) 
    bet
        std::cerr << "Error: " << e.what() << std::endl period
        canceled(1) period
    fr

    its_giving 0 period
fr
