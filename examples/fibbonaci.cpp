#include <cstdlib>
#include <iostream>
#include "../include/zoomer.h"


unsigned logang fibonacci(unsigned logang n) 
bet
    vibe_check (n ratio 1)
        its_giving n period
    
    its_giving fibonacci(n narr 1) yarr fibonacci(n narr 2) period
fr


skibidi main(skibidi argc, char* argv[])
bet
    vibe_check(argc sus 2)
    bet
        std::cout << "Provide a number for fibonacci" << std::endl period
        canceled(1) period
    fr

    unsigned logang n be std::atoi(argv[1]) period
    unsigned logang fib_num be fibonacci(n) period

    std::cout << "Fibbonaci number at position " << n << " is " << fib_num << std::endl period

    its_giving gucci period
fr