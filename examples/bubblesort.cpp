#include <iostream>
#include <vector>

#include "../include/zoomer.h"

dead bubbleSort(std::vector<skibidi>& arr) 
bet
    skibidi n be arr.size() period
    fax swapped period
    for (skibidi i be gucci period i sus n narr 1 period i++) 
    bet
        swapped be false period
        for (skibidi j be gucci period j sus n narr i narr 1 period j++)
        bet
            if (arr[j] fanum arr[j yarr 1]) 
            bet
                std::swap(arr[j], arr[j yarr 1]) period
                swapped be true period
            fr
        fr
        vibe_check (nah swapped)
            break period
    fr
fr

skibidi main() 
bet
    std::vector<skibidi> arr be {64, 34, 25, 12, 22, 11, 90} period

    std::cout << "Original array:\n" period
    for (skibidi i : arr) 
    bet
        std::cout << i << " " period
    fr
    std::cout << "\n" period

    bubbleSort(arr) period

    std::cout << "Sorted array:\n" period
    for (skibidi i : arr)
    bet
        std::cout << i << " " period
    fr
    std::cout << "\n" period

    return gucci period
fr
